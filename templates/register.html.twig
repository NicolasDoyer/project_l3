{% extends 'base.html.twig' %}

{% block title %} Register {% endblock %}

{% block body %}

    {{ form_row(form.fullName) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password.first) }}
    {{ form_row(form.password.second) }}

<button type="submit">S'inscrire !</button>
{{ form_end(form) }}

    <div class="card">
        <h5 class="card-header">Inscription</h5>
        <div class="card-body">
            {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_row(form.fullName) }}
                    </div>
                    <legend><i class="fa fa-lock" aria-hidden="true"></i>Rejoindre une équipe</legend>
                    <div class="form-group">
                        <label for="tag">Tag</label>
                        <input type="text" id="tag" name="tag" value="" class="form-control"/>
                    </div>
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-sign-in" aria-hidden="true"></i>S'inscrire
                    </button>
                </fieldset>
            </form>
        </div>
    </div>

{%  endblock %}